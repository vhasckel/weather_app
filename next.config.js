/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;

module.exports = {
  images: {
    domains: ["openweathermap.org"], // Adicione o hostname permitido aqui
  },
};
